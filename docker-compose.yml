services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: navforge
      POSTGRES_PASSWORD: navforge
      POSTGRES_DB: navforge
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
